"use client"
import type { Metadata, ResolvingMetadata } from "next";
import { Inter } from "next/font/google";
import Script, { Props, ScriptProps } from 'next/script'
import "./globals.scss";
import publicURL from "@/functions/imageURL";
import Header from "@/components/Header";
import { useState } from "react";
import printOut from "@/functions/printOut";

const inter = Inter({ subsets: ["latin"] });

 const metadata: Metadata = {
  title: "Dictionary",
  description: "Generated by create next app",
};


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const [opLanguage, setOpLanguage] = useState(``);

  const setLanguage = (lang:string) => {
    setOpLanguage(lang)
  }

  printOut(`Rendering Root Layout`);

  return (
    <html>
      <body className={inter.className} >
        <Header setLanguage={setLanguage}></Header>


        {children}

        <><Script src={publicURL(`/js/theme.js`)} /></>
      </body>
    </html>

  );
}
